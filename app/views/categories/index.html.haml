%div.wrapper
  %h1 All Categories
  = link_to 'Home', products_path
  %p click to view

  %ul.category-display
    - @categories.each do |category|
      %li
        = link_to category.name, category_path(category)
